import 'package:flutter/material.dart';

class OrderScreen extends StatelessWidget {
  OrderScreen({super.key});

  static const String screenRoute = "OrderScreen";

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('OrderScreen'),
    );
  }
}
